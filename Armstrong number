#include<stdio.h>
int main()
{
	int k=0,n=153,r=0,temp=0;
	printf("enter a num");
	scanf("%d",&n);
	temp=n;
	while(n!=0)
	{
		r=n%10;
		k=k+r*r*r;
		n=n/10;
		
	}
	if(temp==k)
	{
		printf("Armstrong number",temp);
	}
	else
	{
		printf("not armstrong number",temp);
	}
	return 0;
}
